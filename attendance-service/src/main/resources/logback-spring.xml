<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Directorio de logs centralizado en user-service -->
    <property name="LOG_DIR" value="${user.dir}/../user-service/logs"/>

    <!-- Appender para consola -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender principal del servicio -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/attendance-service.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/attendance-service.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender para actividad de usuario (centralizado en user-service) -->
    <appender name="USER_ACTIVITY" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/user-activity.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/user-activity.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} | [ATTENDANCE] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger especial para actividad de usuario -->
    <logger name="USER_ACTIVITY" level="INFO" additivity="false">
        <appender-ref ref="USER_ACTIVITY"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
